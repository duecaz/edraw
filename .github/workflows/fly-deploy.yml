name: Deploy to Fly.io

on:
  push:
    branches: [main]
    paths:
      - "apps/web/**"
      - "apps/room/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/fly-deploy.yml"
  workflow_dispatch:
    inputs:
      target:
        description: "Which app to deploy"
        required: true
        default: "both"
        type: choice
        options: [both, web, room]

jobs:
  deploy-room:
    name: Deploy room (realtime)
    if: github.event_name == 'push' || github.event.inputs.target == 'both' || github.event.inputs.target == 'room'
    runs-on: ubuntu-latest
    concurrency:
      group: fly-room
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - name: Deploy room
        run: flyctl deploy --remote-only --config apps/room/fly.toml --dockerfile apps/room/Dockerfile
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  deploy-web:
    name: Deploy web (frontend)
    needs: deploy-room
    if: always() && (github.event_name == 'push' || github.event.inputs.target == 'both' || github.event.inputs.target == 'web')
    runs-on: ubuntu-latest
    concurrency:
      group: fly-web
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - name: Deploy web
        run: |
          flyctl deploy --remote-only \
            --config apps/web/fly.toml \
            --dockerfile apps/web/Dockerfile \
            --build-arg VITE_ROOM_SERVER="${VITE_ROOM_SERVER}"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          VITE_ROOM_SERVER: ${{ vars.VITE_ROOM_SERVER || 'https://edraw-room.fly.dev' }}
